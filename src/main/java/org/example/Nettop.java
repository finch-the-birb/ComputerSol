package org.example;

public class Nettop extends Computerlmpl{
    public Nettop (String cpu,String ozu,String hdd, String model) {
        setCPU(cpu);
        setHDD(ozu);
        setOZU(hdd);
        setModel(model);
    }
}
